# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: smarttaskmanager
services:
  app:
    language: dotnet
    project: ./src/SmartTaskManager.AppHost/SmartTaskManager.AppHost.csproj
    host: containerapp
